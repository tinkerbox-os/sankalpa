/// Updates browser/PWA chrome where supported.
///
/// Native Flutter targets have their own system chrome APIs, so this is a
/// deliberate no-op outside the web build.
void setBrowserThemeColor(String cssColor) {}
